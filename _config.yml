name: Simple SRD: An easy to use template for quickly creating SRD style documents and webpages.
short_name: Simple SRD
collections:
  rules:
    output: true
plugins:
  - jekyll-seo-tag
remote_theme: non-binary-trees/simple-srd-core
defaults:
  - scope:
      path: ""
      type: rules
    values:
      layout: default
